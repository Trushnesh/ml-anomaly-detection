# Default values for ml-anomaly-detection.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

# image:
#   repository: trushnesh/mlanomalydetection
#   tag: 1.0-SNAPSHOT
#   imagePullPolicy: Always
#   command: [ "echo", "SUCCESS" ]

image:
  repository: nginx
  tag: stable
  pullPolicy: Always

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

kafka:
  bootstrapAddress: 